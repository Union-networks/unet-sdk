# @union-networks/client

Framework-neutral U-net HTTP client, polling helpers, QR helpers, and shared errors.

This package is part of the U-net SDK alpha. See the root docs for integration examples.
