{
  "name": "@union-networks/setup",
  "version": "0.2.0-alpha.0",
  "description": "Local-only U-net provider environment and key setup CLI.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "unet-setup": "dist/cli.js"
  },
  "files": ["dist", "README.md"],
  "sideEffects": false,
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@union-networks/issuer": "workspace:*"
  },
  "devDependencies": { "vitest": "^3.2.4" }
}
