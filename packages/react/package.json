{
  "name": "@unet/react",
  "version": "0.1.0-alpha.0",
  "description": "React hooks and components for U-net login and verification.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "sideEffects": false,
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": { "react": ">=18" },
  "dependencies": { "@unet/client": "workspace:*", "@unet/web-login": "workspace:*", "@unet/verification": "workspace:*" }
}
