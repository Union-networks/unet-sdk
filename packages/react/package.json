{
  "name": "@union-networks/react",
  "version": "0.2.0-alpha.6",
  "description": "React hooks and components for U-net login and verification.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "dependencies": {
    "@union-networks/client": "workspace:*",
    "@union-networks/web-login": "workspace:*",
    "@union-networks/verification": "workspace:*"
  }
}
