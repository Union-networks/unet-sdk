## API Report File for "@u-net/web-login"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { PollOptions } from '@u-net/client';
import { ResolveServiceInput } from '@u-net/client';
import { ServiceResolution } from '@u-net/client';
import { UnetClientOptions } from '@u-net/client';
import { VerifiedService } from '@u-net/client';

// @public
export const createDirectProviderLogin: (origin: string, options?: DirectProviderLoginOptions) => Promise<DirectProviderLoginChallenge>;

// @public
export interface DirectProviderLoginChallenge {
    // (undocumented)
    approvalUrl: string;
    // (undocumented)
    challenge: string;
    // (undocumented)
    challengeUrl: string;
    // (undocumented)
    expiresAtIso: string;
    // (undocumented)
    origin: string;
    // (undocumented)
    protocolVersion: 2;
    // (undocumented)
    requestRef: string;
    // (undocumented)
    serviceId: string;
}

// @public (undocumented)
export interface DirectProviderLoginOptions {
    // (undocumented)
    challengePath?: string;
    // (undocumented)
    fetchImpl?: typeof globalThis.fetch;
    // (undocumented)
    statusPath?: string;
    // (undocumented)
    timeoutMs?: number;
}

// @public
export interface DirectProviderLoginPollResult {
    // (undocumented)
    session?: {
        sessionId: string;
        requestRef: string;
        scopedUserId: string;
        expiresAtIso: string;
    };
    // (undocumented)
    state: 'pending' | 'approved' | 'consumed' | 'expired';
}

// @public (undocumented)
export interface DomainAdministrationProofRequest {
    // (undocumented)
    reasonCode?: string;
    // (undocumented)
    role: DomainAdministrationRole;
    // (undocumented)
    serviceId: string;
    // (undocumented)
    status: 'proving' | 'failed';
    // (undocumented)
    verification?: {
        sessionId: string;
        sessionRef: string;
        nonce: string;
        expiresAt: string;
        requestedChecks: Array<Record<string, unknown>>;
    };
}

// @public (undocumented)
export type DomainAdministrationRole = 'owner' | 'admin';

// @public (undocumented)
export interface DomainAdministrationSelection {
    // (undocumented)
    attestationCommitment: string;
    // (undocumented)
    role: DomainAdministrationRole;
    // (undocumented)
    serviceId: string;
}

// @public (undocumented)
export interface DomainAdministrationSelectionStatus {
    // (undocumented)
    reasonCode?: string;
    // (undocumented)
    role: DomainAdministrationRole;
    // (undocumented)
    serviceId: string;
    // (undocumented)
    status: 'selected' | 'proving' | 'verified' | 'failed' | 'omitted';
}

// @public
export const getDirectDomainAdministrationSelectionStatus: (requestRef: string, options?: UnetClientOptions) => Promise<{
    success: true;
    selections: DomainAdministrationSelectionStatus[];
}>;

// @public
export const getDirectProviderLogin: (origin: string, requestRef: string, options?: DirectProviderLoginOptions) => Promise<DirectProviderLoginPollResult>;

// @public
export const isDirectProviderLoginApproved: (result: DirectProviderLoginPollResult) => boolean;

// @public
export const renderDirectLoginQrPayload: (challenge: DirectProviderLoginChallenge) => string;

// @public
export const resolveDirectLoginService: (input: ResolveServiceInput, options?: UnetClientOptions) => Promise<ServiceResolution>;

export { ResolveServiceInput }

export { ServiceResolution }

// @public
export const submitDirectDomainAdministrationSelections: (challenge: Pick<DirectProviderLoginChallenge, "requestRef" | "expiresAtIso">, selections: DomainAdministrationSelection[], options?: UnetClientOptions) => Promise<{
    success: true;
    results: DomainAdministrationProofRequest[];
}>;

export { UnetClientOptions }

export { VerifiedService }

// @public
export const waitForDirectProviderLogin: (origin: string, requestRef: string, options?: DirectProviderLoginOptions & PollOptions) => Promise<DirectProviderLoginPollResult>;

// (No @packageDocumentation comment for this package)

```
