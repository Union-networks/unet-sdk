# @union-networks/client

Compatibility bridge for the permanent `@u-net/client` package name.

```bash
npm install @u-net/client
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

