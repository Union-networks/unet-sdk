# @union-networks/react

Compatibility bridge for the permanent `@u-net/react` package name.

```bash
npm install @u-net/react
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

