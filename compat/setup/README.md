# @union-networks/setup

Compatibility bridge for the permanent `@u-net/setup` package name.

```bash
npm install @u-net/setup
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

