# @union-networks/server

Compatibility bridge for the permanent `@u-net/server` package name.

```bash
npm install @u-net/server
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

