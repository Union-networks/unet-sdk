# @union-networks/web-login

Compatibility bridge for the permanent `@u-net/web-login` package name.

```bash
npm install @u-net/web-login
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

