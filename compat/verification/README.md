# @union-networks/verification

Compatibility bridge for the permanent `@u-net/verification` package name.

```bash
npm install @u-net/verification
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

