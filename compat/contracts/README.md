# @union-networks/contracts

Compatibility bridge for the permanent `@u-net/contracts` package name.

```bash
npm install @u-net/contracts
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

