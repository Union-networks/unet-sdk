# @union-networks/issuer

Compatibility bridge for the permanent `@u-net/issuer` package name.

```bash
npm install @u-net/issuer
```

This package re-exports the Sovereign Core V2 API. Removed V1 APIs are not restored.

